// LESSON 7, ASSIGNMENTS

// Variable and Value Assignments:

// 1. Declare 3 variables as one-liner:
let firstName = 'John', age = 30, isStudent = true;

// 2. Reassign all 3 declared variables' values:
firstName = 'Jane';
age = 25;
isStudent = false;

// 3. Reassign the value of the second variable to the third variable:
isStudent = age;

// 4. Console.log the third variable's value:
console.log(isStudent);

// 5. Console.log all variables' values:
console.log(firstName, age, isStudent);

// 6. Create 2 variables using `const`:
const pi = 3.14;
const greeting = 'Hello!';


// Variable Naming:

// 1. Create a variable with the name of your city and think how to name it correctly:
const cityName = 'London';

// 2. Create a variable for the present weather conditions, how would you name such a variable?
const presentWeatherConditions = 'sunny';
