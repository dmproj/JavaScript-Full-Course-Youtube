///ASSIGNMENTS, LESSON 15, JAVASCRIPT

// 1. - Create a variable `strOne` and assign it the value of the string "one".
//    - Compare `strOne` with the number `1` using the loose equality operator (`==`).
//    - Log the result of the comparison and the type of the comparison using `console.log()`.
const strOne = "one";
console.log(strOne == 1);

// 2. - Create a variable `hexNumber` and assign it the value `0x14D`.
//    - Compare `hexNumber` with the number `333` using the not equal operator (`!=`).
//    - Log the result of the comparison and the type of the comparison.
const hexNumber = 0x14D;
console.log(hexNumber != 333);

// 3. - Compare `null` with the number `0` using the greater than operator (`>`).
//    - Log the result of the comparison and the type of the comparison.
console.log(null > 0);

// 4. - Compare `null` with the number `0` using the loose equality operator (`==`).
//    - Log the result of the comparison and the type of the comparison.
console.log(null == 0);

// 5. - Compare `null` with the number `0` using the greater than or equal to operator (`>=`).
//    - Log the result of the comparison and the type of the comparison.
console.log(null >= 0);

// 6. - Compare `null` with `undefined` using the loose equality operator (`==`).
//    - Log the result of the comparison and the type of the comparison.
console.log(null == undefined);

// 7. - Compare the number `0` with `undefined` using the loose equality operator (`==`).
//    - Log the result of the comparison and the type of the comparison.
console.log(0 == undefined);

